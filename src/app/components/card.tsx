export function Card({ movies }: any) {
  return (
    <>
      {/* <ul>
        {movies.map((movie: any) => (
          <li key={movie.id}>
            <h3>{movie.title}</h3>
          </li>
        ))}
      </ul> */}
    </>
  );
}
